<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Application layer: CQRS commands/queries, handlers, DTOs, validators, mapping
    profiles and MediatR pipeline behaviors. Depends only on the Domain layer.
    Cross-cutting libraries (MediatR, FluentValidation, AutoMapper) are added in
    the feature/application-layer branch.
  -->

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RentFlow.Domain\RentFlow.Domain.csproj" />
  </ItemGroup>

</Project>
