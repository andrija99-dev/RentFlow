<Project Sdk="Microsoft.NET.Sdk.Web">

  <!--
    API layer: the ASP.NET Core host. Controllers, middleware, composition root.
    References Infrastructure (which transitively brings in Application + Domain).
    Feature-specific packages (JWT, versioning, rate limiting, health checks) are
    added in their respective feature branches.
  -->

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
    <PackageReference Include="Scalar.AspNetCore" />
    <PackageReference Include="Serilog.AspNetCore" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RentFlow.Infrastructure\RentFlow.Infrastructure.csproj" />
  </ItemGroup>

</Project>
